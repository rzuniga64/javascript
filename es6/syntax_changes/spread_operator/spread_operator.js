// The spread operator converts an array of numbers into a list of numbers.
// It looks like rest but depends on where you place it.

let numbers = [1, 2, 3, 4, 5];

console.log(...numbers);
console.log(Math.max(...numbers));
